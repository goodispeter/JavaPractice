ns=100 s=125
ns=0 s=125
ns=125 s=125

1. s因為是static變數 所以可以排除掉s不一樣的答案
備註:static變數屬於全域共享變數,該變數的值只有一份,會被所有該類的實例共想

2. 再來看ns,是屬於non-static變數,每個類的實例都會擁有自己的一份該變數,並且不互相影響
也就是說當沒有執行this.ns = ns 這段程式時,還未實例化的ns都屬於原始值(沒有賦值的話預設為0)